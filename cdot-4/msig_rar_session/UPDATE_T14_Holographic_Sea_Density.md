# UPDATE — T14: Sea Density from Holographic Saturation — $a_0$ Predicted to ~12%

*Proposed update to T14 (§"Toward the RAR", §"Energy Scale", Open Item 5). Status: the
emission-accumulation route (both number- and mass-conservation) FAILS by exactly
$1/f_\text{dot}=10^{39}$. Replaced by a holographic saturation condition that gives the sea
density as an **exact, $\hbar$-free algebraic identity** $\rho_\text{bg}=(\pi/6)\rho_\text{crit}$,
predicting $a_0=(3/16)\,cH_0$ — within 2–11% of the measured value, ~12% ($9/8$) above the
$cH_0/6$ target, with **no free amplitude**. A new derived link (holographic saturation),
not confirmation of the prior framing. Session: 2026-07-01.*

---

## 1. Emission Accumulation Fails — Decisively and Informatively

Number-conservation accumulation of the connecton sea from the emission rate ($f_\text{dot}
\sim10^{-39}$ of rest energy per Hubble time, T14) gives
$$U_\text{sea} = n_c E_c = \frac{U_\text{emit}}{E_c}\cdot E_c = U_\text{emit}
= f_\text{dot}\,\rho_m c^2 \;\Rightarrow\; \frac{\rho_\text{bg}}{\rho_\text{crit}}\sim 3\times10^{-40}.$$
The per-connecton energy $E_c$ **cancels**, so the sea energy just equals the emitted energy —
short of critical by **exactly $1/f_\text{dot}=10^{39}$**. Mass-conservation routes through the
same feeble rate and fails identically. *Conclusion: the connecton sea is not the reservoir of
emitted connecton energy under any conservation law.* The exact $1/f_\text{dot}$ shortfall is
the proof.

(This answers the fallback the user flagged: mass conservation does not rescue it — the
shortfall is in the energy rate, common to both.)

---

## 2. The Sea Is a Holographic Standing Population — Not an Accumulating One

The connecton sea is conserved and re-emitted (T14): a **standing** population whose density
is set by a steady-state condition, not an accumulating reservoir. Anchoring it to the
horizon's holographic degrees of freedom:
$$N_\text{hor} = \frac{4\pi R_0^2}{4L_p^2},\quad L_p^2=\frac{\hbar G}{c^3},\qquad
n_\text{holo} = \frac{N_\text{hor}}{V_\text{hor}} = \frac{3}{4L_p^2 R_0},$$
each carrying the minimal IR quantum mass $m_c = \hbar H^\text{hor}/c^2 = \hbar H_0/(2c^2)$.
Then
$$\rho_\text{bg} = n_\text{holo}\,m_c = \frac{3\hbar H_0}{8c^2 R_0 L_p^2}
= \frac{3H_0 c}{8 R_0 G} \xrightarrow{R_0=6c/H_0} \frac{H_0^2}{16 G}.$$

**$\hbar$ cancels exactly.** The holographic count $\propto 1/\hbar$ times the minimal quantum
mass $\propto\hbar$ is $\hbar$-independent. So this is not a quantum coincidence — it is the
structural statement that the horizon's holographic dof, each carrying the IR quantum,
reproduce a critical-density-scale sea regardless of $\hbar$:
$$\boxed{\,\rho_\text{bg} = \frac{H_0^2}{16G} = \frac{\pi}{6}\,\rho_\text{crit}\quad(\text{exact}).\,}$$

---

## 3. The $a_0$ Prediction

Feeding $\rho_\text{bg}=(\pi/6)\rho_\text{crit}$ into the $1/r$-profile floor
$g_\dagger = G\rho_\text{bg}R_0/2$:
$$a_0 = \frac{G\rho_\text{bg}R_0}{2} = \frac{3}{16}\,cH_0 = 0.1875\,cH_0.$$
Target $cH_0/6 = 0.1667\,cH_0$; ratio $= 9/8$ (**12.5% high**). Versus the **measured** $a_0$:

| $H_0$ | predicted $a_0$ | pred/measured |
|---:|---:|---:|
| 67.4 | $1.23\times10^{-10}$ | 1.02 |
| 70.0 | $1.28\times10^{-10}$ | 1.06 |
| 73.0 | $1.33\times10^{-10}$ | 1.11 |

Within the McGaugh systematic band across the Hubble range; essentially exact at Planck $H_0$.
**No free amplitude** — the sea density is fixed by the holographic identity.

---

## 4. Honest Status — What Improved, What Remains

**Improved (major):** last session left $S$ as "O(1), dimensionally forced, undetermined."
It is now a specific prediction from holographic saturation: $a_0=(3/16)cH_0$, no free
amplitude, within ~12% of target and inside the measured band. The emission rate $f_\text{dot}$
is shown to be a red herring for the sea density (the exact $1/f_\text{dot}$ shortfall proves
it).

**Remaining gap — the $9/8$ (12.5%):** comes from stacking three individually-derived
geometric factors — the holographic $\pi/6$, the profile factor $1/2$, and $R_0=6c/H_0$. The
residual means one of them carries an uncorrected O(1):
- The **profile factor $1/2$** is the least rigorous — it came from a specific reading of the
  $1/r$ charge integral ($Q=\int(B/4\pi r)4\pi r^2dr = Br^2/2$). A different boundary/geometry
  could shift it.
- The **Bekenstein-Hawking normalization** $A/4L_p^2$ has a known O(1) ambiguity in the
  coefficient.
- The **quantum mass** $m_c=\hbar H^\text{hor}/c^2$ uses the horizon rate ($H_0/2$); the
  observable rate would shift it by $P=2$.
A $9/8$ from these is plausibly a single mis-set factor, not missing physics — but it is not
resolved here.

**New assumption flagged:** the holographic-saturation anchor ("the connecton sea saturates
the horizon's holographic bound") is a **new** link, not previously in T14. It is
well-motivated (it makes $\hbar$ cancel and lands on $a_0$) but should enter as a new derived
premise, not as confirmation of the earlier "connecton = dark-energy quantum" framing (which
gave only an O(1) motivation). Notably, the two are consistent: $(\pi/6)\rho_\text{crit}$ is
of order $\rho_\Lambda$, so the holographic sea IS dark-energy-scale — now with a specific
coefficient.

---

## 5. Suggested Document Edits (on merge)

- **T14 §"Energy Scale"** — add: the sea is a holographic standing population;
  $\rho_\text{bg}=(\pi/6)\rho_\text{crit}=H_0^2/16G$ ($\hbar$-free); emission accumulation is
  ruled out (fails by $1/f_\text{dot}$). Mark holographic saturation as a new premise.
- **T14 §"Toward the RAR"** — $a_0=(3/16)cH_0$, within ~12% of $cH_0/6$ and inside the
  measured band, no free amplitude. State the residual $9/8$ and its likely origin (profile
  factor / BH normalization / horizon-rate choice).
- **T14 Open Item 5** ("connecton density → G explicit") — partially addressed: the sea
  density is now pinned holographically to $(\pi/6)\rho_\text{crit}$. Reframe as: reconcile
  this with the $G$-setting condition and resolve the $9/8$.
- **Core Principles §7** — RAR/M-σ line: "shape derived; $a_0=(3/16)cH_0$ from a holographic
  sea density $\rho_\text{bg}=(\pi/6)\rho_\text{crit}$ ($\hbar$-free identity), within ~12% of
  $cH_0/6$ and inside the measured band, **no free amplitude**; residual $9/8$ from geometric
  factors, unresolved. Emission accumulation ruled out."
- **Supersedes** the "$S$ undetermined" conclusion of `UPDATE_T14_Sea_Amplitude_Partial.md`:
  $S$ is now predicted ($=9/8$ relative to target), not merely bounded — though the $9/8$
  itself is not yet derived to be 1.

---

## 6. The Next Computation

Resolve the $9/8$. In order of likelihood:
1. **Redo the $1/r$-profile charge factor** rigorously (the $1/2$), including the correct
   inner boundary and the connecton-charge-to-acceleration mapping — most likely source.
2. **Fix the horizon-rate vs observable-rate** choice in $m_c$ consistently with the $R_0$
   definition.
3. **Pin the holographic coefficient** (whether $A/4L_p^2$ or a connecton-specific count).
If these resolve to unity, $a_0=cH_0/6$ is derived with no free parameter and the RAR is
complete in shape and scale. If a stubborn O(1) remains, it is the theory's single
normalization input — but now a factor of $9/8$, not an unbounded $S$.
