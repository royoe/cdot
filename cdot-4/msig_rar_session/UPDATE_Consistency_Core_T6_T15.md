# UPDATE — Consistency Pass: Core §7, T6, T15 Are Stale vs. the Current T14/T17

*Consolidated cross-file correction. T14 and T17 are current and internally consistent
(the connecton-gravity thread merged faithfully). Three files still carry the **pre-thread**
framing and now contradict T14: Core Principles §7 (status table), T6, and T15. All three
predate the derivation of the RAR closure, the $q=2$ spectrum, the $1/r$ profile, and the
holographic coefficient. Corrections specified below. Session: 2026-07-01.*

---

## 1. What Is Current (no action needed)

- **T14** — fully current. The chain (indistinguishability closure → $q=2$ cascade →
  holographic sea density $\rho_\text{bg}=(\pi/6)\rho_\text{crit}$ → $a_0=(3/16)cH_0$) is
  merged with the honest $9/8$ residual flagged everywhere it appears. Internally consistent.
- **T17** — current (per the earlier consistency pass: $B_c$ baryon-sourced, BH a co-tracer,
  M-σ derived, orbital-coherence factor). Consistent with T14.
- **Core §0–§6, §4a, §5** — current and correct. The two-Hubble-rate machinery
  ($H_0^{\text{obs}}=P\,H_0^{\text{hor}}$, $R_0=6c/H_0$) is exactly what the coefficient work
  used. No changes.

---

## 2. Core Principles §7 — Status Table Is Stale (5 rows)

The §7 table still reports the RAR/connecton/M-σ program at its pre-thread status. Specific
rows to replace:

- **Line 316** — "Radial Acceleration Relation / MOND functional form: **Open — scale
  matches, mechanism absent**" →
  "**Substantially derived** — RAR closure derived from connecton indistinguishability
  (0.020 dex vs McGaugh, alternatives data-excluded); $a_0=(3/16)cH_0$ from a holographic
  sea density $\rho_\text{bg}=(\pi/6)\rho_\text{crit}$ ($\hbar$-free), inside the measured
  band, no free amplitude; residual $9/8$ vs $cH_0/6$ target open (T14, T15, T6)."
- **Line 320** — "MOND constant $a_0$ value: **Open** (T6)" →
  "$a_0=(3/16)cH_0$ predicted (holographic sea density); ~12% above $cH_0/6$, inside
  measured band; residual $9/8$ open (T14, T6)."
- **Line 323** — "Dynamical selection / ring ejection: **Needs re-derivation**…" →
  "**Re-derived under invariant G** as a direct time-steady Lorentz velocity filter; flatness
  as attractor; attractor convergence open (T14, T17)."
- **Line 325** — "Connecton gravity — foam-sea gives Newtonian $1/r$; **MOND not
  reproduced**: **Speculative**" →
  "Connecton gravity — Newtonian $1/r$ from foam diffusion; **RAR closure and $a_0$
  coefficient substantially derived**; speculative but materially advanced (T14)."
- **Line 328** — "Galaxy morphology and M-σ … **Speculative; re-derivation needed**" →
  "Galaxy morphology and M-σ — M-σ **derived** as the BTFR through the shared $a_0$; $B_c$
  baryon-sourced (BH a co-tracer); orbital-coherence factor predicts M-σ scatter vs
  rotational support; speculative but advanced (T17)."

Also update the **header note (lines 11–13)**: "the rotation-curve mechanism … has been
demoted — it fails at galactic scale by $\sim10^6$ (T5); dark-matter-free rotation curves are
an open problem, not a result." This refers to the *retardation* mechanism (T5), which is
correctly dead — but the sentence now reads as the model's overall verdict and is contradicted
by T14. Suggest: "the *retardation* rotation-curve mechanism is demoted (fails by $\sim10^6$,
T5); the *connecton/foam-diffusion* route instead derives the RAR closure and pins $a_0$ to
$(3/16)cH_0$ (T14) — speculative but the leading dark-matter-free direction."

---

## 3. T6 — Coefficient and "Mechanism Absent" Language Stale

T6 is written around "$a_0\approx cH_0/2\pi$, coincidence real but unexplained, no mechanism."
Every one of these is now superseded:

- **Lines 33, 35, 57, 211, 219** — the $cH_0/2\pi$ coefficient. Replace with $cH_0/6$
  throughout: the geometric derivation gives exactly $6=3P$ from the horizon radius, and
  $2\pi\approx6.28$ was a numerical near-miss. Note $2\pi$ fits *worse* (needs $H_0\approx78$
  vs $\approx74$ for 6). The holographic route sharpens this to $a_0=(3/16)cH_0$.
- **Lines 66–67, 86, 105** — "the coincidence is real but unexplained"; "no mechanism
  currently exists"; "linear in $M$… fails." Replace: the mechanism now exists — the RAR
  closure is derived from indistinguishability (T14); the non-analytic wall fell to the
  transition-radius geometric mean; the coefficient is holographically pinned.
- **Line 181** — the M-σ "criticality-as-license" reading (BH provides a critical surface).
  Superseded: M-σ is derived as the BTFR through the shared $a_0$ (T14/T17), BH a co-tracer,
  not a critical-surface license. Keep the *conclusion* (BH doesn't set the amplitude) but
  replace the mechanism.
- **Lines 209–221 (Open Questions)** — "first-principles derivation of $a_0=cH_0/(2\pi)$ or
  $cH_0/(4\pi)$"; "the factor $1/2\pi$: geometric integration?" Replace the open question with
  the current one: the coefficient is $cH_0/6$ (horizon geometry) → $(3/16)cH_0$ (holographic
  sea); the remaining open item is the residual $9/8$, not the existence of a derivation.

T6's core dimensional content ($a_0\sim cH_0$ is the natural scale) is correct and stays; only
the "$2\pi$ / unexplained / no mechanism" wrapper is stale.

---

## 4. T15 — Same Stale Wrapper

T15 (the theory-neutral RAR statement) is mostly fine as an empirical description, but its
model-status claims are stale:

- **Lines 44, 111, 143** — $cH_0/2\pi$ and "derive $a_0\approx cH_0/(2\pi)$ with the correct
  coefficient." Replace with $cH_0/6$ → $(3/16)cH_0$; the open item is the $9/8$ residual.
- **Lines 65, 100–103, 118** — "no viable order-unity galactic mechanism exists"; "held in
  abeyance until a mechanism exists." Superseded by the derived closure (T14). Reclassify from
  "no mechanism" to "closure derived; deepening tasks (transport kernel, attractor
  convergence, $9/8$) remain."
- **Line 111** — the cross-reference "T6: derive $a_0\approx cH_0/(2\pi)$" should point to the
  holographic derivation in T14 §"Energy Scale".

The empirical RAR content (the McGaugh relation, the 0.13 dex scatter, the theory-neutrality)
is correct and unchanged.

---

## 5. Possible Changes to Core Premises (the substantive question)

The thread introduced one genuinely new structural element that rises to premise level, plus
two that do not. Recommended:

**(a) Consider adding — holographic sea density as a derived bridge.** The identity
$\rho_\text{bg}=(\pi/6)\rho_\text{crit}=H_0^2/16G$ ($\hbar$-free) links the connecton sea to
the critical/dark-energy density with a fixed coefficient. This is currently in T14 as a "new
premise," but it connects three core-level quantities ($a_0$, the horizon $R_0$, and
$\rho_\Lambda$) and reframes the long-standing $a_0\sim cH_0\sim c^2\sqrt{\Lambda/3}$
coincidence as a *single structural identification* rather than three separate facts. **This
belongs in Core §0 or §6 as a candidate relational result**, flagged speculative — it is the
first thing in the program to tie the MOND scale and the dark-energy scale to the same object.
Recommended wording: a note that "in the connecton picture the MOND acceleration, the horizon,
and the dark-energy density are the same order-unity identification, $a_0\sim cH_0\sim
G\rho_\Lambda R_0$, because the connecton sea is simultaneously the dark-energy background and
the MOND floor source (T14)." Mark speculative until the $9/8$ closes.

**(b) Do NOT elevate — the RAR closure and $q=2$ spectrum.** These are strong T14 results but
remain speculative (connecton ontology unproven, attractor convergence open). They belong in
T14, not Core. The §7 table rows (§2 above) are the right level of Core visibility.

**(c) Do NOT change — premises 1–4.** Nothing in this thread touches the four foundational
premises. The connecton indistinguishability that powers the RAR closure is a property *within*
the speculative connecton hypothesis, not a new foundational premise. Core premises stable.

**(d) Flag for §0 conservation discussion.** The failed emission-accumulation calculation
(sea ≠ emitted-energy reservoir; number and mass conservation both fail by $1/f_\text{dot}$)
is a useful negative result. It belongs in T14 (already noted), but §0's "interpretation of
the energy change (connecton exchange vs …) is an open modelling choice" could add a
one-clause pointer that the connecton sea is a holographic standing population, not an
energy-accumulating one (T14).

---

## 6. Priority

1. **Core §7 table (5 rows) + header note** — highest priority; it is the model's front-page
   status and currently contradicts T14/T17 directly.
2. **T6 and T15 coefficient ($2\pi\to6\to3/16$) and "mechanism absent" language** — these read
   as the model failing at exactly the thing T14 now does; internally contradictory as-is.
3. **Core premise addition (a)** — the holographic $a_0$–$\rho_\Lambda$ identity as a
   speculative relational result. Substantive and worth surfacing, but flagged speculative.
4. **§0 pointer (d)** — minor.

No changes to T14, T17, or Core §0–§6/§4a/§5, which are current.
