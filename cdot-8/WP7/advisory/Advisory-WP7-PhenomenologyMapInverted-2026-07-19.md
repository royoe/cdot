# Advisory — WP7 Reality Check Adjudicated: the Worker's $R_h(z_*)$ Catch Is Conceded in Full as Advisor Error #8 — and the Corrected Map Doesn't Shrink the M5 Phenomenology, It Inverts It: the Signature Moves from the First Peak to the Growth History, Where It Is Order-One (for `cdot-8/WP7/`)

*2026-07-19. Advisory in response to `wp7_anchor_brackets.py` and the
updated WP7 file. Corrected map in `wp7_phenomenology_map.py`. Gate 1(b)
carried. Verdict up front: **conceded, ledgered as advisor error #8. The
$kR_h\sim6$ "first acoustic peak" entry — used illustratively since §2a
and allowed to harden into "the first peak carries an ~8% window, must
be carried" across two advisories — was never checked against the actual
$R_h(t)$ and is wrong by five orders of magnitude. Error class: an
external-framework value (ΛCDM's $\sim285$ Mpc comoving horizon)
imported into a variable-$c$ trajectory without computing — in a
framework where $c\propto a^{2/3}$ makes the early causal ball
$\sim100\times$ smaller before trajectory differences. The exact class
this program polices, caught by the worker with the literal WP2
definition. Their numbers are confirmed independently. But the right
conclusion is not "M5 is irrelevant to the CMB" — with the true
$R_h(t)$, the window sweeps down through $k$ as $R_h$ grows, and the
phenomenology map inverts: every observable mode was fully M5-coupled
($W\approx1$) through recombination, exits during the matter era, and
the distinctive $W$-shape survives only at $\ell\lesssim10$ via the
late-time evolution. The new central finding: during each mode's coupled
era the M5 response modifies the Poisson source at $O(0.1$–$1)$ — the
growth history, not the acoustic peaks, is where cdot-8's
perturbation-level signature lives.***

---

## 1. The concession, and the confirmation

$R_h(z_*)=3.3\times10^{-3}$ Mpc on the literal WP2 definition
($\dot R_h=c(t)$, $c=c_0a^{2/3}$, integrated on the exact trajectory) —
against $r_s=173$ Mpc and $D_p=13{,}074$ Mpc, so $kR_h(z_*)\sim10^{-6}$–
$10^{-5}$ for every observable multipole: **the worker's computation is
confirmed to the digit, and their statement that the illustrative table
"was never checked against the actual $R_h(z)$ trajectory" is exactly
right.** Ledger #8, class named: ΛCDM-value import without computing.
The physics behind the smallness deserves one honest sentence in the
record: with $c$ growing as $a^{2/3}$, light crawled at early times —
the causal ball is tiny in exactly the regime where ΛCDM intuition says
"horizon-scale." Variable-$c$ frameworks of the growing-$c$ kind have
*small* early horizons; the illustrative table assumed the opposite
without noticing it was an assumption.

## 2. The corrected map — inversion, not erasure

The window's argument $kR_h(t)$ is epoch-dependent, and with the true
$R_h(t)$ each mode has an **exit epoch** $R_h(t_k)=1/k$: $W\approx1$
(fully coupled) before it, decoupled after. On the trajectory:
$z_{\rm exit}\approx6.5$ for the first-peak $k$, $\approx16$ for
$k=0.1\,{\rm Mpc}^{-1}$, $\approx56$ for cluster scales; the lowest
observable multipoles ($\ell\lesssim10$) are still transitioning today.
Three regimes replace the old picture:

1. **At recombination, all observable modes sit at $W\approx1$**: the
   field-side $(1-W)$ term vanishes there ($\sim10^{-12}$, not the 8%
   two advisories claimed — both to be errata'd), and the Einstein-side
   term is at full strength but *scale-independent* — and at $W=1$ it is
   precisely the separate-universe-consistent linear response of the
   cdot-8 background (the same $Q$-drag the background Friedmann
   equation carries). **Including it is required for consistency; it
   carries no distinctive $k$-shape at $z_*$.** Anchor 1 was telling us
   this all along.
2. **The distinctive $W$-shape is a late-time, $\ell\lesssim10$
   feature** (ISW-era), where $kR_h(t)\sim1$ today — the "low-$\ell$
   signature" survives, relocated and re-derived through the late
   evolution rather than recombination.
3. **The new central item — growth**: during each mode's coupled era the
   M5 response modifies the Poisson source by
   $(F_Q/6+QF_{QQ}/2)\,q'\bar{\mathcal N}$ with elasticity
   $d\ln Q/d\ln\mathcal N\approx-0.29$ (computable: $d\ln Q/ds=-5/2$
   against the census engulfment rate $3\,d\ln R_h/ds$) — coming out at
   **$O(0.6)$ of the matter source through the matter era** in this
   first estimate, with the exact number owed to the worker's assembly
   under the two anchors. Order-one, not a refinement: **the growth
   history ($\sigma_8$-class observables) replaces the acoustic peaks as
   WP7's central phenomenological deliverable**, and — Gate 1(b) framing
   intact — as its central risk.

**Re-scoped statement for the record** (replacing "clean sub-horizon
import"): *the AeST system imports cleanly for each mode after its exit;
during the coupled era the separate-universe M5 response must be
carried.* WP5/WP6 are untouched — $kR_h({\rm today})\sim10^5$–$10^9$ for
quasistatic scales, confirmed in the script.

## 3. Directives

1. Errata for the two advisories carrying the 8%-first-peak claim; the
   illustrative-table lesson joins the fold-in queue (illustrative
   values must be labelled unrunnable-on or computed — nothing in a
   table that can harden).
2. The assembly's first numerical target is now the coupled-era growth
   equation: the $W\approx1$ M5 source with the elasticity computed from
   the trajectory (radiation-class censuses added to the matter-class
   engulfment used in the estimate), evolved through each mode's exit;
   $\sigma_8$-class output under Gate 1(b) framing.
3. The late-time $\ell\lesssim10$ derivation follows, with the window-
   shape band and facet-4 band as previously directed — both freedoms
   now confined to the late/very-large-scale regime, which the corrected
   map makes explicit.
4. The mode-exit function $z_{\rm exit}(k)$ goes into the toolchain
   alongside the worker's $R_h(s)$ integration.

## 4. Housekeeping

Ledger: #8 recorded. The worker's practice — running the literal
definition against a number that had been repeated twice — is the
program working; that both #7 and #8 were advisor errors caught by the
worker in consecutive rounds is itself information the author should
have: the error-catching is genuinely bidirectional, and the loop is
healthy. Consolidation-batch sighting still expected. KATRIN unchanged.
Nothing in `cdot-7/` touched.

## Companion

- `wp7_phenomenology_map.py` — the confirmation, the exit history, the
  growth coefficient estimate.
- This advisory: proposed location
  `cdot-8/WP7/Advisory-WP7-PhenomenologyMapInverted-2026-07-19.md`.
