# Advisory — WP2 Addendum Reviewed: Accepted — the Decision Is Correct on the Record and Self-Consistent as the Minimal Covariantization; One Amendment (the Declined Alternative Is Facet 4 of the Freedom Item, Not Outside It) and One Sharpening (the Matter Census Is Immune by the $p=5/2$ Cancellation) (for `cdot-8/WP2/` and `cdot-8/WP7/`)

*2026-07-19. Advisory in response to
`Update-WP2-Addendum-CensusWeightQDependence-2026-07-19.md`. Verification
in `census_weight_review.py`. Gate 1(b) carried. Verdict up front: **the
addendum is accepted. Every textual claim verifies — $E_P(t)$'s
per-slice argument list, the pre-$Q$ kinematic status of
$c(t)=c_0(a/a_0)^{2/3}$, the $p_i^{\rm sp}$ derivations and their match
to the $g_i$ structure on record — and the decision is the right kind:
made on the definitions as written, in writing, exactly as directed. Two
refinements before it's filed. First, the addendum's scope note draws
the line one notch too cleanly: the alternative it correctly declines —
a locally-normalized census, $E_P$ built from the ball-smoothed local
$c$ — is not excluded by physics, only by the definitions as built, and
therefore belongs inside the census-sector covariantization freedom as
its fourth facet, not outside it. Second, and usefully: that facet is
bounded harder than generic — the matter census is exactly immune to it,
by the same $p_m=5/2$ cancellation that zeroes $g_{\rm matter}$'s
$\dot c/c$ term on the background.***

---

## 1. Accepted — with the verification run

The reading of the record is sound and complete: $E_P(t)$ is a per-slice
normalization by definition, same status as $\Lambda_M(t)$ and $\bar Q(t)$
in $S_{M5}$; $c(t)$ is WP1's kinematic relabeling relation, derived
before $Q$ or $F(Q)$ exist in the program, background-only as written;
the $p_i^{\rm sp}$ are fixed structural exponents with nowhere for a
local field value to enter, and they reproduce the $g_i$ coefficients on
record ($p_m-\tfrac52=0$, $p_\gamma-\tfrac52=-\tfrac32$ ✓). The
consequence stands: **the density+volume decomposition of
$\delta\mathcal N_i$ is complete at linear order under the definitions
as written — no third channel.** The assembly proceeds on it. The
closing paragraph distinguishing background-$\bar Q$ dependence (fully
carried in $\bar{\mathcal N}_i(t)$) from an independent perturbative
channel is exactly the right distinction, exactly where confusions of
this kind breed.

## 2. The amendment — facet 4, inside the freedom item

The addendum says the decision "does not bear on" the covariantization
freedom. One notch too separate: what the decision actually does is
**adopt the minimal covariantization of the census normalization**
(one $E_P$ per slice, the ball-center observer's Planck units) and
decline the alternative (a locally-normalized census, $E_P(x,t)$ from
the smoothed local $c$, which would close a self-consistency loop
$\delta c\propto q'\delta\mathcal N$ back into $\delta\mathcal N$ —
a coefficient renormalization within the same window architecture, not
new structure). That alternative is not wrong physics — it is a
*choice*, unmade by the record until this addendum made it. So it files
as **facet 4 of the census-sector covariantization freedom**
(gauge status, fiducial center, volume convention, normalization
locality), with the addendum's decision recorded as the declared
default. Same pattern as every facet: default stated, alternative
banded, anchors untouchable.

## 3. The sharpening — matter immunity bounds the facet

How large can facet 4 be? The census integrand per species is
$\rho_{E,{\rm coord}}/E_P\propto c^{p_i-5/2}$, so under the local-$c$
alternative a smoothed $\delta c$ enters species $i$'s census as
$(p_i-\tfrac52)\,\delta c/c$. **For matter, $p_m-\tfrac52=0$ — exactly
immune**: the same cancellation that zeroes the background
$\dot c/c$ term in $g_{\rm matter}$ kills the perturbative channel too.
Only the radiation-class censuses ($p-\tfrac52=-\tfrac32$) can feel the
facet. Consequence for WP7: **the matter-era low-$\ell$ structure is
convention-free on facet 4; the freedom touches only the
radiation-era/crossover end of the term** — the freedom item shrinks
precisely where the late-time CMB lives. This bound goes into the
freedom item's write-up next to the two anchors, as the third thing the
freedom cannot touch.

## 4. Directives

1. File the addendum with §2's amendment: facet 4 recorded inside the
   freedom item, addendum's decision as the declared default.
2. Add the matter-immunity bound (§3) to the freedom item — anchors:
   $k\to0$ continuity, sub-horizon decoupling, and now matter-census
   invariance under facet 4.
3. WP7 assembly proceeds unchanged on the density+volume decomposition;
   the radiation-era coefficient band inherits facet 4's width when the
   low-$\ell$ numbers are quoted.

## 5. Housekeeping

Fold-in queue: +this review's amendment and bound. Consolidation-batch
sighting still expected. KATRIN watch item unchanged. Nothing in
`cdot-7/` touched — and the addendum's own discipline on that point is
noted.

## Companion

- `census_weight_review.py` — the verification run and the immunity
  table.
- This advisory: proposed location
  `cdot-8/WP2/Advisory-WP2-AddendumReviewed-2026-07-19.md`.
