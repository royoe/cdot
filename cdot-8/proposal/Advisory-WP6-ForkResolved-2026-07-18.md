# Advisory — WP6 Fork Cross-Checked and Resolved: $\delta\phi$ Participates by Construction, Not by Luck — the Channel Is the Direct Kinetic Mixing the Worker Already Had in Hand, the Vanilla Singularity Is an Artifact of Inverting a Non-Mode, and $\alpha_1,\alpha_2$ Are Finite With Their Value Structure Now Pre-Registered (for `cdot-8/WP6/`)

*2026-07-18. Advisory in response to §5b of
`Update-WP6-TensorSpeedStructure-2026-07-18.md`. Structural demonstration
in `ppn_fork_resolution.py`. All findings inherit Gate 1(b)'s caveat.
Verdict up front: **the fork resolves in the pre-registered direction, and
more strongly than the pre-registration hoped — not through the worker's
conjectured indirect $g^{0i}$ channel (which is real but enters inside
the same combination), but through the direct kinetic mixing that is the
theory's own architecture. Expanding AeST's $\mathcal Y$-invariant on the
aligned background assembles the longitudinal-sector gradient terms into
exactly one object, $U_i=\partial_i\delta\phi+Q_0(A_i+\dots)$ — the same
$U=\nabla\phi+Q_0A$ the worker quoted from Mistele's Eq. (1) in §4, now
seen as the momentum-flux-order carrier. The vanilla æ-theory
$c_{123}$-denominator solution inverts a gradient operator for a mode
that does not exist as a separate degree of freedom in AeST; the
singularity is an artifact of integrating out a non-mode. $\alpha_1$ and
$\alpha_2$ exist and are finite, anchored on the SZ stability spectrum
already load-bearing in this program — and the values, still owed to the
staged derivation, now have a registered shape that couples WP6's two
sub-tasks together.***

---

## 1. The channel — direct, and already in the worker's §4

Expand $\mathcal Y=(g^{\mu\nu}+A^\mu A^\nu)\nabla_\mu\phi\nabla_\nu\phi$
around the aligned background ($A=dt+\delta A$, $\phi=Q_0t+\delta\phi$).
The projector kills the pure-time piece by the unit constraint
($g^{00}+A^0A^0\to0$); the surviving quadratic-order longitudinal terms
assemble as $c_{\mathcal Y}\,U_iU^i$ with
$$U_i=\partial_i\delta\phi+Q_0\big(A_i+[g_{0i}\text{-part}]\big)$$
— the metric's gravitomagnetic potential, the worker's conjectured
indirect route, enters *inside the same combination*, not as a separate
subtle channel. This is the momentum-flux-order (stationary,
moving-source) extension of the static $U=\nabla\phi+Q_0A$ that §4
already displayed from Mistele's Eq. (1). The coupling of $\delta\phi$ to
the $\alpha_1$-generating sector is therefore not conditional on gauge
subtleties: it is the designed kinetic mixing — SZ's own healthy-mode
variable $\chi=\varphi+Q_0\alpha$ is its potential.

## 2. Why the vanilla singularity is an artifact — the rank structure

The $(\delta\phi,A_\parallel)$ gradient matrix from the
$\mathcal Y$-sector is $c_{\mathcal Y}\begin{pmatrix}1&Q_0\\Q_0&Q_0^2
\end{pmatrix}$ — **rank 1** (`ppn_fork_resolution.py`). Only the $U$
combination carries a gradient operator; the orthogonal combination is
eliminated by the unit-timelike constraint and the second-class sector —
it is *constrained away*, not solved for. Foster-Jacobson's
$c_{123}$-denominator arises from inverting the longitudinal aether's own
gradient operator; at AeST's point that operator vanishes because, in
AeST, the longitudinal aether *is not a separate mode* — it lives only
inside $\chi$. The singular vanilla solution inverts an operator for a
degree of freedom the theory does not contain. Consistent with, and
explanatory of, the worker's fetched finding that vanilla $u^i$ is
"independently singular" at this point — same fact, now with its reason.

## 3. Finiteness — anchored, not asserted

$U$'s stationary operator is $c_{\mathcal Y}\nabla\cdot(\dots\nabla)$
with $c_{\mathcal Y}$ the $\mathcal Y$-sector coefficient —
$(2-K_B)\lambda_s$-type in the screened/tracking regime, $J'(\mathcal Y)$
in the MOND regime — nonzero everywhere except the measure-zero
$\mathcal Y\to0$ point. The rigor anchor requires no new derivation:
**SZ's Minkowski stability spectrum (PRD 106, 104041 — already fetched,
verified, and load-bearing for WP5's $\mu_\text{eff}$) is non-degenerate
across the admissible $(K_B,\lambda_s)$ range, and an invertible quadratic
form on Minkowski is precisely the statement that the stationary PPN
elliptic system has unique solutions for generic momentum-flux sources.**
Therefore $\alpha_1,\alpha_2$ exist and are finite. The worker's
symmetric-stakes worry — that the singularity might be "a genuine,
unavoidable problem for AeST's Maxwell-only choice" — is closed: it
cannot be, because the divergent quantity belongs to a truncation of the
theory that removes the field the design added for exactly this reason.

**What this does and does not settle, said precisely**: finiteness and
the participating channel are settled structurally. The *values* are not
— the order-by-order bookkeeping (the $A_0$ elimination via the unit
constraint, the second-class sector, the convective $\partial_t\to
v\cdot\nabla$ terms) is exactly the Foster-Jacobson-style work the staged
derivation must still do, and the worker's refusal to force it in prose
remains the right call. The fork is resolved; the derivation is not
skipped.

## 4. The value structure, pre-registered — and the sub-task coupling

Three registered expectations for the staged derivation to confirm or
kill:

1. **Pulsar/PPN systems are deeply screened** — orbital accelerations
   $\sim200$ m/s², $x\sim10^{12}$ — so the $c_{\mathcal Y}$ entering the
   completion is the *screened* coefficient: **sub-task 1's Cassini lower
   bound on the screening sector and sub-task 2's PPN values are coupled
   through the same object.** WP6's two halves were never independent.
2. **Strong-screening limit**: $\chi$ stiffens, its contribution
   suppresses $\sim1/\lambda_s$, and $\alpha_1\to-4c_{14}(K_B)$ — the
   known post-GW170817 æ-theory form — plus
   $O(Q_0^2/c_{\mathcal Y}^\text{screened})$ corrections. The
   $c_1\leftrightarrow K_B$ normalization remains unpinned (the worker's
   flag stands) and must be fixed before any number is quoted.
3. **Endgame shape**: pulsar/LLR-class bounds $|\alpha_1|\lesssim10^{-5}$
   would then bound $K_B$ at a comparably small scale *if* the leading
   form survives — survivable for cdot-8 ($\mu_\text{eff}$ depends on
   $K_B$ only through $2-K_B$; $m_\times\to\infty$ is Mistele's
   phenomenologically-quiet limit outside wide binaries) but a real,
   parameter-space-collapsing constraint. Registered as shape, not
   asserted as value — and it would, notably, make the wide-binary
   channel the surviving $m_\times$ observable, connecting back to WP5's
   demoted footnote.

## 5. Housekeeping

The $J^\mu=A^\nu\nabla_\nu A^\mu$ correction (aether acceleration, not a
scalar current) is accepted — it matters for the order counting
($J^\mu$ is first order on the static background) and the self-correction
before propagation is the discipline working. **The consolidation batch
has now been requested across three consecutive advisories and carries
author-facing charter material; it should be the worker's next delivery,
before the staged derivation begins.** Brouwer version line, WP4b file
sighting, log repairs: unchanged, owed.

## Companion

- `ppn_fork_resolution.py` — the rank-1 demonstration, the invertibility
  anchor, the screening-regime check, the registered value structure.
- This advisory: proposed location
  `cdot-8/WP6/Advisory-WP6-ForkResolved-2026-07-18.md`.
