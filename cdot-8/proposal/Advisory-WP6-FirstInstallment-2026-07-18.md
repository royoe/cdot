# Advisory — WP6 First Installment Assessed: the Tensor Import Is Sound and Strengthened; the M5-Tensor Question Answered at Zero-Mode Level; and Sub-Task 1 Reframed Before It Walks Into a Known Kill — the Program's Own $\mu$ Is the Excluded Simple Function (for `cdot-8/WP6/`)

*2026-07-18. Advisory in response to
`Update-WP6-TensorSpeedStructure-2026-07-18.md`. Numerics in
`mu_swap_exposure.py`. Every statement below inherits Gate 1(b)'s
provisional-structural-failure caveat on the cosmological background, per
the author's standing instruction. Verdict up front: **§1's import is
verified and accepted; §2's named assumption is confirmed and upgraded —
the M5 term itself (not just $F(Q)$) exerts zero force on the tensor
sector at linear order, by the same variational argument WP5 established,
and the GW170817-class observable is invariant under the framework's
global $c(t)$ evolution by construction. §3's ordering is right, but
sub-task 1 as scoped walks straight into a collision the worker hasn't
named: the closure's own fitted $\mu(x)=x/(1+x)$ IS the simple
interpolating function T22 excluded by $\sim2800\times$. That is a known
property of the naked simple function, not a waiting discovery — the
correct object of the Cassini test in AeST-chassis language is the
screening sector, and the $\mu$-swap exposure lands, with a flag worth
registering, at the same tens-of-percent scale as WP4a's $\theta_*$
miss.***

---

## 1. §1 accepted — with one addition each way

The tensor-speed import is verified against sources already in this
loop's record: SZ's second-order tensor action (stability paper, their
Eq. 20) is *identically* GR's, and the founding paper's requirement (v)
plus the class-level result (Skordis–Złośnik 2019, PRD 100, 104013) make
"in all situations" a field-content statement, not a parameter choice.
Two additions:

- **The $c(t)$ point, stated so it never becomes a confusion**: cdot-8's
  global $c(t)$ evolution does not touch the GW170817-class observable,
  because that observable is the *relative* arrival time of GW and photon
  signals riding the same metric light cones — a ratio, invariant under
  K1's global evolution by construction. cdot-8 satisfies the constraint
  identically, at every epoch, for the same structural reason AeST does.
- **A registrable future-facing prediction, per Gate 2's stated aim**:
  with a constant bare $\tilde G$ tensor-sector prefactor and no running
  Planck mass, cdot-8 predicts $d_L^{\rm GW}/d_L^{\rm EM}=1$ exactly —
  standard-siren consistency — where much of the modified-gravity
  literature predicts deviations. Cheap to state now, testable by
  LISA/ET-era sirens; belongs in the prediction registry alongside WP5's.
  (The deeper two-clock question — what cdot-8's redshift mechanism does
  to chirp-mass inference — is flagged as WP6-adjacent, *not* scoped in.)

The PPN literature gap is confirmed as the worker states it; the Vaglio
et al. 2026 anchor is accepted as WP0's verified finding.

## 2. §2's named assumption — confirmed, and upgraded to cover M5 itself

The sector-additivity argument covers $F(Q)$'s modification; the worker's
flag correctly notes it hasn't been re-derived at perturbation level. Two
closures, both from machinery already established in this loop:

1. **$F(Q)$ cannot enter the tensor sector at quadratic order**: tensor
   perturbations $H_{ij}$ appear in neither $Q=A^\mu\nabla_\mu\phi$ nor
   $\mathcal Y$ at linear order ($A_\mu$ and $\phi$ carry no tensor
   part), so $\delta^2F$ contains no $H_{ij}$ — SZ's Eq. 20 being exactly
   GR is the explicit demonstration, and nothing cdot-8 changes about
   $F$'s functional form can alter which fields enter it.
2. **M5 itself exerts zero tensor-sector force at linear order** — the
   piece the worker's argument didn't cover: $\bar Q$ is a scalar
   functional (no tensor part in $\delta\bar Q$), and $\delta\mathcal
   N_{\rm tot}$ from tensor modes is second order (GW energy density) and
   horizon-diluted — the WP5 variational argument applies verbatim, with
   an even cleaner conclusion (spatially uniform *and* zero at linear
   order, rather than merely uniform).

The named assumption is therefore discharged, not just plausible: verdict
**$c_{\rm gw}=c_\gamma$ holds in cdot-8 in all situations**, carrying only
Gate 1(b)'s background caveat, which does not touch this sector.

## 3. Sub-task 1, reframed before it runs — the $\mu$ collision

The worker's first sub-task is "check which interpolating-function shape
cdot-8's quasistatic limit settles into." The answer is already in the
program's own records, and it is the collision: **the closure machinery
has used $\mu(x)=x/(1+x)$ — the simple function — continuously since
WP2** (it is inside every trajectory integration in this loop's scripts,
including the $E(z)$ backbone). The naked simple function's slow
Newtonian return ($1-\mu\sim1/x$) is exactly what T22 excluded at
$\sim2800\times$; running T22's machinery against it will "discover" a
known result. Pre-registered reframing (`mu_swap_exposure.py`):

- **(a) The correct object of the Cassini test is AeST's screening
  sector, not the naked $\mu$ tail.** The chassis has its own
  GR-restoration machinery (SZ: "two ways GR can be restored — screening
  and tracking"; $\lambda_s$, higher-$\mathcal Y$ terms), which is
  precisely what suppresses Solar-System MOND residuals in AeST
  regardless of the galaxy-regime interpolation shape. Expected
  deliverable: a *lower bound on the screening sector* from
  Cassini/T22-class ranging, which then feeds sub-tasks 2–3 — not a
  pass/fail on $\mu_{\rm simple}$.
- **(b) If the naked-$\mu$ framing is run anyway, the $2800\times$
  reappears — it is T22's known property of $\mu_{\rm simple}$, not
  news**, and must not be booked as a new cdot-8 kill.
- **(c) The closure's exposure to a Cassini-safe $\mu$-swap is real and
  quantified**: at the trajectory's working points ($x_0=1.10$, fixed
  points 1.72/3.44, $x(1100)=2.61$), Cassini-safe families sit **24–41%
  above** $\mu_{\rm simple}$ — a $\kappa\lambda/x_0$ refit at the
  tens-of-percent scale. Quantify, don't execute, until the post-WP7
  revisit — **where the $\mu$-swap now goes first in the options queue,
  for a registered reason: it moves closure-era quantities at the same
  tens-of-percent order as WP4a's 27% $\theta_*$ miss, and unlike every
  other candidate it is externally forced (Cassini) rather than invented
  to fix $\theta_*$.** Direction unverified — it may equally worsen
  $\theta_*$ — so this is a pre-registered candidate, not a fix. But two
  independent experimental pressures pointing at the same object is
  exactly what the Gate 1(b) investigation should test first.

## 4. Sub-tasks 2–3 — guidance on record before they start

- **The Einstein-æther $\alpha_1,\alpha_2$ formulas cannot be lifted
  directly.** AeST's aether sector maps onto æ-theory's kinetic
  coefficients, but the $\phi$–$A$ coupling and the $\chi=\varphi+Q_0\alpha$
  mixing (SZ Eq. 25) have no æ-theory counterpart — which is *why* the
  literature gap exists. The derivation must extend the PPN machinery to
  include the scalar mixing; the æ-theory results are the scaffold, not
  the answer. Budget accordingly: this is WP6's real work.
- **WP5's $\mu^{-1}\approx5$–$10$ Gpc directly cleans the pulsar
  sector**: the condensate contributes nothing at AU–pc scales
  ($r_c\sim$ 60–100 Mpc for galaxy masses; unmeasurably larger cutoffs
  for stellar masses), so the strong-field sensitivity functions adapt
  from the æ-theory literature without condensate contamination — a
  simplification vanilla AeST (Mpc-scale $\mu$) cannot claim.
- One cdot-8-specific $\alpha_1/\alpha_2$ note: preferred-frame effects
  reference the aether's cosmological rest frame; the evolving background
  $Q(z)$ adds only adiabatic drift ($\sim H$), second-order small against
  the pulsar systems' velocities. Not a loophole, just a line for the
  write-up.

## 5. Housekeeping

1. **The KATRIN line in the worker's preamble is stale**: the "specific
   pending action" was resolved in `SessionLog-2026-07-18.md` Entry 2 —
   registered falsification criterion ($m_\beta\approx0.458$ eV vs the
   published 0.45 eV bound; final sub-0.3 eV analysis pending; watch item
   routed to the author on arrival). Sync and strike the "unclarified."
2. **The companion log "to be created" already exists** — two entries as
   of this morning's consolidation. Entry-9 rule: continue numbering,
   don't fork a fresh file.
3. Still owed: the Brouwer version statement (one line), the WP4b file
   sighting, the consolidation batch (now also carrying this round's
   $\mu$-collision pre-registration and the siren prediction).

## Companion

- `mu_swap_exposure.py` — the working-point comparison table, the
  exposure verdict, the magnitude-coincidence flag, the three
  pre-registered expectations.
- This advisory: proposed location
  `cdot-8/WP6/Advisory-WP6-FirstInstallment-2026-07-18.md`.
