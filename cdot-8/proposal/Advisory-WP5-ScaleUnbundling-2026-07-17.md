# Advisory — WP5 Scale Unbundling: The Worker's $m_\times$ Finding Is Confirmed Exactly, but the Tested Hypothesis Bundled Two Distinct Scales — the Advisor's Error; Unbundled Against the Primary Source, the Lensing-Tension Attribution Reverses, the RAR Curve Unblocks, and the Real Zero-Freedom Task Emerges (for `cdot-8/WP5/`)

*2026-07-17. Advisory in response to §6a of
`Update-WP5-WeakFieldStructure-2026-07-17.md`. Adjudicated against the
fetched full text of the primary source (Mistele, "A new scale in the
quasi-static limit of Aether Scalar Tensor Theory," arXiv:2305.07742v3,
Phys. Rev. D 110, 024062), not either party's memory of it. Skeleton
numerics in `meff_skeleton.py`. Verdict up front, in three parts: **(1)
the worker's core finding is confirmed exactly — $m_\times=Q_0\sqrt{(2-K_B)
/(2K_B)}$ has no dependence on the free function, so replacing $F(Q)$
cannot remove it, and the pre-registered hypothesis as I stated it fails;
(2) but the hypothesis I pre-registered bundled two distinct scales that
the paper explicitly separates, and the worker inherited the bundle — the
known weak-lensing tension traces to $m$ (the ghost-condensate mass, which
IS the dust-mimicking device cdot-8's charter discards), not to $m_\times$,
so the tension-persistence conclusion attaches to the wrong scale and the
distinguishing-advantage question is back open through the $m$-channel;
(3) the corrected question is sharp and zero-freedom: cdot-8's action has
no native $m^2$ term, but the quadrature-determined $F$ generates an
effective condensate mass $m_\text{eff}^2\sim\tfrac12Q_0^2F_{QQ}(Q_0)$ —
computable, with a pre-registered skeleton estimate landing at Hubble
scale, $\sim7000\times$ lighter than AeST's chosen value, which would put
the condensate cutoff far outside every survey radius. Separately, the
worker's blocking recommendation is over-cautious: the paper's own
quantitative conclusion is that galaxy-scale observables differ
sub-percent between the two limits, so the lensing-RAR curve is
unblocked.***

---

## 1. Confirmed: the $m_\times$ core finding, verbatim from the source

The worker's transcription of Eq. (1) is exact, and so is the reading:
$m_\times\equiv Q_0\sqrt{(2-K_B)/(2K_B)}$ (the paper's Eq. 4/21) is built
from the aether's spatial kinetic coefficient $K_B$ and the $\phi$–$A$
coupling $Q_0$ only. The free function enters nowhere in it; the paper
states plainly that $J$ determines the interpolation function and that
$m_\times$ "is not related to the ghost condensate... and does not affect
the ability of AeST to reproduce MOND." cdot-8's modification lives
entirely inside the free function; therefore cdot-8 inherits $m_\times$
and the two-limit structure. **My pre-registered hypothesis, as stated,
fails, and the worker's test of it was correctly executed.** That stands.

## 2. The advisor's error: the hypothesis bundled two scales — ledger entry #6

My §4 directive last round attributed *both* "the two-limit ambiguity and
the lensing-tension departure" to a single object I called "the
$\mu$-function mass term $m_\times$." The paper explicitly refuses that
bundling, in its introduction and throughout: **$m$ — the ghost-condensate
mass — "controls whether AeST reproduces MOND,"** sets the critical radius
$r_c=(r_M f_G/m^2)^{1/3}$ beyond which the condensate density spoils MOND,
and is the parameter that Mistele–McGaugh–Hossenfelder 2023 (A&A 676,
A100) constrain with weak-lensing data — *that* is the known tension. And
$m$ is precisely the dust-mimicking device: the $m^2(\dot\phi/Q_0-\Phi)^2$
term is the condensate's energy density, the thing that makes AeST's
scalar gravitate as dark matter cosmologically. **$m_\times$, by contrast,
"is, to the best of our knowledge, not yet constrained phenomenologically"**
(paper, §V) — it produces no lensing tension at all. So:

- The two-limit question traces to $m_\times$ → cdot-8 inherits it ✓
  (worker's finding).
- The weak-lensing tension traces to $m$ → the sector cdot-8's charter
  discards → **the worker's "the associated known weak-lensing tension it
  produces very likely persists in cdot-8 exactly as in vanilla AeST" is
  not supported by the source — wrong scale.**

The bundling error is mine, recorded as advisor error #6 (source
misattribution; the worker inherited it and tested the bundle faithfully).
The unbundled question — does cdot-8 carry the *$m$-term*? — is the one
that was worth asking all along, and it has a sharper answer than either
of us expected (§4 below).

## 3. The $Q_0$ "notational collision" — a real catch, but over-corrected

The worker is right that Mistele's $Q_0$ is a constant within the
quasistatic action (mass dimension 1) and must not be conflated with a
dynamical variable inside the quasistatic equations. But "entirely
different object, unrelated referent" is too strong: the paper itself says
$\dot\phi$ "represents the chemical potential of this condensate" and
allows only $\dot\phi=\text{const}$ — Mistele's $Q_0$ is the **frozen
background value of the same condensate chemical potential that cdot-8
evolves as $Q(t)$**. Same object, adiabatically frozen for local physics —
exactly as $a_0(z)$ is frozen per epoch in §3's recovered equation. The
consequence is real, not notational: in cdot-8, $m_\times$ and the
$m_\text{eff}$ of §4 inherit epoch dependence through $Q(z)$. For
$m_\times$ this is a footnote (it is phenomenologically near-irrelevant,
§5); for $m_\text{eff}$ it matters (the condensate cutoff $r_c(z)$ at lens
epochs is part of the second installment).

## 4. The corrected task, and its pre-registered skeleton: $m_\text{eff}^2$ from $F_{QQ}$ — zero freedom, decisive either way

cdot-8's closed action (WP1–WP3, audited) contains $F(Q)$ and the
census/M5 sector only — **no native $m^2$ term was ever written**. But in
Newtonian gauge the perturbation of the invariant is
$\delta Q=\delta\dot\phi-Q_0\Phi$ — exactly the structure inside AeST's
$m^2(\dot\phi/Q_0-\Phi)^2$ — so expanding the quadrature-determined $F$ to
second order *generates* an effective condensate mass,
$$m_\text{eff}^2\sim\tfrac12\,Q_0^2\,F_{QQ}(Q_0)\quad\text{(up to the
careful }f_G/16\pi\tilde G\text{ dictionary)},$$
**with zero freedom — $F$ is fixed by the invoice.** This is the genuine
cdot-8-specific question, and it cuts decisively either way: AeST can tune
$m$ small to soften the MMH tension; cdot-8 cannot tune anything. If the
careful pass lands $m_\text{eff}^{-1}\lesssim$ Mpc, cdot-8 inherits the
lensing tension with no escape — kill-relevant for G1. If it lands far
above survey radii, the distinguishing advantage returns, quantitative and
honest.

**Skeleton estimate (`meff_skeleton.py`; advisor pre-registration, NOT the
careful pass):** from the established quadrature (inline closed-form check:
matter-era $F/\Omega_s=+1.82$ vs target $+1.77$ ✓),
$F_{QQ}(Q_0{=}1,\text{today})=-0.696$ in $H_0^2$ units, giving
$$m_\text{eff}\sim1.4\times10^{-4}\,\text{Mpc}^{-1}\quad
(1/m_\text{eff}\approx7300\,\text{Mpc}),$$
roughly $7000\times$ lighter than AeST's chosen $m\sim1\,\text{Mpc}^{-1}$
— because cdot-8's $F$ is Hubble-scale *by construction* (it supplies the
invoice), whereas AeST's $m$ was chosen Mpc-scale to make the scalar
cluster as dark matter, a job cdot-8's census does instead. The condensate
cutoff for a $10^{11}M_\odot$ lensing-stack galaxy
($r_M\approx10$ kpc) comes out $r_c\sim(r_M/m_\text{eff}^2)^{1/3}\approx
80$ Mpc — **far beyond the 1–3 Mpc that stacked lensing probes → condensate
negligible at all survey radii → MOND persists → consistent with the very
MMH 2023 data that pressures vanilla AeST.** Pre-registered as the
expectation the careful pass must confirm or kill.

**Assignment, with the caution my own record earns:** the normalization
chain (our $H_0^2$-unit $F$ ↔ Mistele's quasistatic $m^2$; the
$\delta Q\leftrightarrow(\dot\phi/Q_0-\Phi)$ dictionary; the $f_G$ and
$16\pi\tilde G$ factors) is precisely the cross-frame computation class
where this advisor has erred before — the skeleton is a target, not a
result. Two specific flags for the careful pass: (i) $F_{QQ}<0$ here —
work out the sign convention and its stability implication explicitly
rather than absorbing it; (ii) evaluate $m_\text{eff}(z)$ along the
trajectory, not just today, since $r_c(z)$ at lens epochs feeds the
confrontation.

## 5. The blocking recommendation is over-cautious: the paper itself unblocks the RAR curve

The worker carried forward "the two-quasistatic-limit question... needs
its own resolution before the lensing-RAR-vs-lens-redshift curve can be
built with confidence." The source says otherwise, quantitatively: the
value of $m_\times$ makes almost no difference for accelerations in
galaxies — sub-percent, "purely theoretical interest" for rotation curves
(its Figs. 1–2) — with the percent-level exception being **wide binaries**,
which are not WP5's deliverable. At survey lens scales
($l\sim$ tens of kpc, $m_\times\lesssim1\,\text{Mpc}^{-1}$ →
$m_\times l\ll1$), the two-field limit applies and reproduces MOND
wherever the condensate is negligible — which is §4's question, not
$m_\times$'s. **Directive: the lensing-RAR-by-lens-redshift curve proceeds
now, in the two-field limit, on the delivered $E(z)$ backbone, with a
stated sub-percent $m_\times$ systematics line. The two-limit question
stays open as a wide-binary footnote, not a WP5 blocker.**

## 6. Directives

1. Record the unbundling: correct §6a's tension-persistence sentence to
   attach the MMH 2023 tension to $m$, with the paper's own distinguishing
   quotes cited; soften the $Q_0$ flag per §3. Advisor error #6 goes to
   the consolidation ledger.
2. Second installment, first task: **the careful $m_\text{eff}$
   normalization pass** (§4), against the pre-registered skeleton, signs
   and $z$-dependence included.
3. Second installment, second task: the lensing-RAR-by-lens-redshift
   confrontation (unblocked per §5) against the stacked literature —
   Brouwer et al. 2021 (KiDS-1000) and Mistele et al. 2024
   (arXiv:2310.15248) are the anchors.
4. Standing: the author's Foundation §6 item 6 decision on WP4a remains
   the gate; worker confirmation of the WP4b normalization adjudication
   and the errata chain remain owed; log repairs (07-16 overwrite,
   numbering, Entry-9 on the 07-17 file) in one delivery; KATRIN clock
   unchanged, most time-critical.

## Companion

- `meff_skeleton.py` — $F_{QQ}$ from the quadrature, the $m_\text{eff}$
  and $r_c$ skeleton, inline closed-form check.
- This advisory: proposed location
  `cdot-8/WP5/Advisory-WP5-ScaleUnbundling-2026-07-17.md`.
