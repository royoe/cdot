# Advisory — WP6 Sub-task 2, Final Piece Answered at the Structural Level: the Driven Response Inherits the Screening Suppression (Step 3e Confirmed); the Advisor's Own Pre-Registration Corrected in Direction; and a Conservative Envelope Delivered That Unblocks Sub-task 3 Today (for `cdot-8/WP6/advisory/`)

*2026-07-18. Advisory in response to the open request at the close of
`Update-WP6-PPNDerivation-2026-07-18.md`. Structural derivation in
`longitudinal_response.py`. All results inherit Gate 1(b)'s caveat.
Verdict up front, in three parts: **(1) Step 3e's hypothesis is confirmed
at the structural level — the momentum-flux response is governed by the
same stiff operator whose stiffness produces the static suppression, and
none of the O(1.5) sources is screening-enhanced, so
$\delta U\sim\varepsilon\times O(1.5)$. (2) In consequence, the advisor's
own earlier pre-registration ("strong screening recovers
$\alpha_1\to-4c_{14}$") was wrong in direction and is corrected on the
record: at AeST's parameter point the vanilla æ-theory $\alpha_1$ is
carried entirely by the longitudinal channel AeST replaces, so strong
screening does not restore the æ-value — it suppresses the only channel
that produced it. Corrected expectation:
$\alpha_1(\text{cdot-8})=O(K_B\varepsilon)$, plus at most one
unsuppressed $O(K_B)$ candidate (the "E-term") whose provenance mixes in
vanilla substitutions and is the single remaining certified-derivation
item. (3) A conservative envelope — $|\alpha_1|\le4K_B$ — is quotable
today with its basis stated, yielding
$K_B\lesssim2.5\times10^{-6}$ (pulsar) as a conservative bound under
which cdot-8 passes every preferred-frame test with all previously
established structures intact. Sub-task 3 is unblocked on that
conditional basis; the exact $\alpha_1,\alpha_2$ remain open, honestly
and boundedly.***

---

## 1. The driven response — Step 3e confirmed, with the mechanism

Linearizing Step 2's corrected equation around the screened static
background $U^{(0)}$, the response operator is
$$\nabla\cdot[\,\mathbb M\,\delta U\,]=\delta S,\qquad
\mathbb M_{ij}=(1+\tilde\mu)\delta_{ij}+\tilde\mu'|U^{(0)}|\,\hat U_i\hat U_j.$$
In the screened regime *both* eigenvalue families are large — $\sim\tilde\mu$
transverse to $U^{(0)}$, $\sim\tilde\mu+\tilde\mu'|U^{(0)}|$ along it, with
no soft direction for tracking/screening completions
($\tilde\mu'|U|\ge0$). The $O(1.5)$ sources, enumerated from the
covariant structure: the $Q_0h_{0i}$ piece inside $U$ (frame dragging
feeding the scalar — the fork round's channel, now with its job), the
convective $\partial_t\to v\cdot\nabla$ terms, the $J$-term pieces, and
the $F_QA^\mu$ term, which is $(H_0\ell)^2$-suppressed and dropped with
statement. **None is $\tilde\mu$-enhanced.** Hence
$\delta U\sim\varepsilon\,\delta S$ with
$\varepsilon=1/\tilde\mu_{\rm screened}$: *the driven response inherits
the static suppression because it is the same stiff medium being driven.*
Step 3e's physical argument is upgraded from hypothesis to structural
result — with the one caveat that "structural" here means the operator
and scaling are derived; the $O(1)$ coefficient is not.

## 2. The directional correction — the advisor's own

Step 3d proved the transverse sector is exactly GR at AeST's point. So
vanilla æ-theory's $\alpha_1=-4c_1$ *at this same point* is carried
entirely by its longitudinal $u^L$ channel — the $c_{123}$-singular one,
finite only through a $0\times\infty$ cancellation. AeST replaces that
channel with the screened $\chi/U$, whose stress feed into $g_{0i}$ is
doubly small ($T_{0i}\sim c_{\mathcal Y}U_0U_i$ with $U_0$ itself
perturbation-suppressed). **Therefore the earlier registered
expectation — that the strong-screening limit recovers the æ-theory form
$-4c_{14}$ — was wrong in direction: screening does not restore the
æ-value, it suppresses the only channel that produced it.** Recorded as
a corrected pre-registration (it was registered as shape, hedged, never
quoted as a result — but the ledger records directional corrections to
expectations exactly as it records errors in results, because
downstream work steers by them). The corrected expectation:
$$\alpha_1(\text{cdot-8})=O(K_B\,\varepsilon)\;\;[+\text{ at most one
}O(K_B)\text{ candidate, §3}].$$
Physically: **the same screening that protects Cassini protects the
pulsars** — sub-tasks 1, 2, and 3 are one mechanism seen three times.

## 3. The one remaining certified item, and the envelope that doesn't wait for it

FJ's $g_{0i}$ equation carries a finite aether-stress coefficient
($E=K_B$ at our point) whose derivation pre-substituted vanilla's $u^L$
solution — content AeST replaces. Whether any unsuppressed $O(K_B)$
piece survives an AeST-native re-derivation of that term is the **single
remaining certified-derivation item** — bounded in scope (one term, one
equation) and bounded in consequence: it can restore at most the vanilla
magnitude. That bound is the basis of the quotable envelope:
$$|\alpha_1|\le4K_B$$
— the replaced channel cannot exceed the channel it replaces under a
stiff operator (an envelope with stated basis, not a theorem). Under it:
**pulsar-class $|\alpha_1|<10^{-5}$ gives $K_B<2.5\times10^{-6}$,
conservative** — and every established cdot-8 structure survives
arbitrary small $K_B$ ($1/\mu_{\rm eff}\to10.3$ Gpc; $m_\times\to\infty$,
one-field limit; WP5 unchanged; tensor/vector sectors $K_B$-benign). The
provisional $\alpha_2$ envelope gives $K_B\lesssim4\times10^{-10}$ from
the solar-spin bound — flagged provisional because $\alpha_2$'s
coefficient needs the same E-term re-derivation; do not quote as final.

**Sub-task 3 is therefore unblocked today, on a conditional statement of
exactly the kind the author's instruction anticipated**: cdot-8 passes
every preferred-frame test for $K_B$ below the envelope, with the
registered expectation that the true constraint is weaker by
$\sim1/\varepsilon$; any pulsar result quoting the envelope carries the
conditionality flag verbatim, per the worker's own §-closing commitment.

## 4. Scope honesty — matching the worker's

The worker stopped because the two-scale perturbative solve around
$U^{(0)}$ has no published anchor and no independent cross-check; this
advisory's Parts 1–2 supply the operator, the scaling, and the corrected
direction, but not the certified $O(1)$ coefficients — the same standard
applied to itself. Closing the exact $\alpha_1,\alpha_2$ needs the
E-term's AeST-native re-derivation plus the $\delta U$ coefficient, as a
dedicated piece of work with a WP3-style checkpoint rhythm, jointly
cross-checked. It is the natural first item if the author ever
commissions a "WP6b exact closure"; nothing downstream is blocked
without it.

## 5. Housekeeping

Fold-in queue for next sync grows by: the directional pre-registration
correction (§2, ledgered), the driven-response structural result (§1),
the envelope with its basis (§3). Consolidation-batch file sighting
still expected. Sub-task 3 proceeds per the author's instruction with
the conditional statement above. KATRIN watch item unchanged. Nothing in
`cdot-7/` was touched.

## Companion

- `longitudinal_response.py` — the operator, the source enumeration, the
  scaling, the corrected expectation, the envelope numbers.
- This advisory: proposed location
  `cdot-8/WP6/advisory/Advisory-WP6-LongitudinalResponse-2026-07-18.md`.
